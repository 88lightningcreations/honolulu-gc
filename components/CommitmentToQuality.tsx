import React from 'react';
import styles from './CommitmentToQuality.module.css';

const CommitmentToQuality = () => {
  return (
    <section className={styles.commitmentSection}>
      <div className={styles.contentWrapper}>
        <h2 className={styles.title}>A Commitment to Quality</h2>
        <p className={styles.description}>
          We don&apos;t just build projects; we build dreams. Our commitment to quality ensures that every detail is perfect, from the foundation to the final fixture.
        </p>
      </div>
    </section>
  );
};

export default CommitmentToQuality;
